# DMLegoArm

SAP Digital Manufacturing Lego Arm Demo

## Pod Plugin

The `pod-plugin/` directory contains plugin assets (manifest, HTML, JavaScript, CSS, and property files) that integrate with the main server. See [`pod-plugin/README.md`](pod-plugin/README.md) for build and deployment instructions.
